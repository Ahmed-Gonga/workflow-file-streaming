diff --git a/fs/nfs/read.c b/fs/nfs/read.c
index d4db23a1f349..e0bd2c04960f 100644
--- a/fs/nfs/read.c
+++ b/fs/nfs/read.c
@@ -7,7 +7,7 @@
  * Partial copy of Linus' read cache modifications to fs/nfs/file.c
  * modified for async RPC by okir@monad.swb.de
  */
-
+#include <linux/delay.h>
 #include <linux/time.h>
 #include <linux/kernel.h>
 #include <linux/errno.h>
@@ -32,6 +32,8 @@
 
 #define NFSDBG_FACILITY		NFSDBG_PAGECACHE
 
+extern loff_t snfs_available_until;
+
 const struct nfs_pgio_completion_ops nfs_async_read_completion_ops;
 static const struct nfs_rw_ops nfs_rw_read_ops;
 
@@ -148,9 +150,15 @@ static void nfs_read_completion(struct nfs_pgio_header *hdr)
 		struct folio *folio = nfs_page_to_folio(req);
 		unsigned long start = req->wb_pgbase;
 		unsigned long end = req->wb_pgbase + req->wb_bytes;
-		pr_info("sNFS READ partial chunk: start=%lu end=%lu bytes=%u\n",
-	start, end, req->wb_bytes);
-
+		
+	/* sNFS prototype: reader consumes an available chunk */
+		pr_info("sNFS STREAMING: reader consumed available chunk [%lu - %lu]\n", start, end);
+	if (end > snfs_available_until) {
+	pr_info("sNFS BLOCK prototype: requested_end=%lu available_until=%lld\n",
+		end, (long long)snfs_available_until);
+
+	msleep(500);
+	}
 		if (test_bit(NFS_IOHDR_EOF, &hdr->flags)) {
 			/* note: regions of the page not covered by a
 			 * request are zeroed in nfs_read_add_folio
diff --git a/fs/nfs/write.c b/fs/nfs/write.c
index 1d89ae2bd47a..776f13ac0b97 100644
--- a/fs/nfs/write.c
+++ b/fs/nfs/write.c
@@ -44,6 +44,10 @@
 #define MIN_POOL_WRITE		(32)
 #define MIN_POOL_COMMIT		(4)
 
+/* sNFS prototype: global available offset for a single streaming file */
+loff_t snfs_available_until = 0;
+EXPORT_SYMBOL_GPL(snfs_available_until);
+
 struct nfs_io_completion {
 	void (*complete)(void *data);
 	void *data;
@@ -1308,7 +1312,12 @@ trace_nfs_update_folio(inode, offset, count);
 
 pr_info("sNFS WRITE partial chunk: file=%pD offset=%u count=%u absolute=%lld\n",
 	file, offset, count, (long long)(folio_pos(folio) + offset));
+/* sNFS prototype: mark written region as available */
+snfs_available_until = folio_pos(folio) + offset + count;
 
+pr_info("sNFS AVAILABLE update: file=%pD available_until=%lld\n",
+	file, (long long)snfs_available_until);
+	
 dprintk("NFS:       nfs_update_folio(%pD2 %d@%lld)\n", file, count,
 	(long long)(folio_pos(folio) + offset));
 
